<p id="notice"><%= notice %></p>

<div class="widget-duelo-tempo-real">
    <div class="container">
        <h3 class="titulo-duelo">Vote em quem você acha que esta ganhando a luta</h3>

        <div class="container-barra-andamento">
            
        </div>

        <div class="container-participantes">
            <% @duelo_pessoa.each do |duelo_pessoa| %>
                <div class="participante">
                    <img class="imagem-participante" src="<%= duelo_pessoa.pessoa.imagem %>" alt="">
                    <p class="nome-participante"><%= duelo_pessoa.pessoa.nome %></p>
                    <button class="btn btn-voto">Bom golpe</button>
                </div>
            <% end %>
        </div>

        <div class="container-historico">
            
        </div>
    </div>
</div>